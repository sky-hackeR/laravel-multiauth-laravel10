@extends('{{LowerName}}.layout.app')

@section('content')
<div class="container">
    <h2>Reset {{Name}} Password</h2>
    <form method="POST" action="{{ url('/{{LowerName}}/password/email') }}">
        @csrf
        <input type="email" name="email" placeholder="Enter Email Address" required>
        <button type="submit">Send Password Reset Link</button>
    </form>
    <p><a href="{{ url('/{{LowerName}}/login') }}">Back to Login</a></p>
</div>
@endsection